const producto = 'Monitor 20 pulgadas';

console.log(producto)

// SABEMOS CUANTAS LETRAS TIENE UN STRING
console.log(producto.length) // muestra un number con el número de letras de nuestro string

// SABER SI EXISTE UNA PALABRA EN UN STRING
console.log(producto.indexOf('20')); // te regresa (muestra) la posicion en la que esta la palabra
console.log(producto.includes('20')) // te regresa un valor Boolean, true si existe la palabra o false si no existe

