const producto = "Monitor 20 pulgadas\""; // Primer forma (string primitivo)

const producto2 = String('Monitor de 24 pulgadas'); // Segunda forma de crear un string (String prtimitivo)

const producto3 = new String('Monitor de 27 pulgadas'); // Tercerea forma de crear un string (crea un objeto string)

console.log(producto);
console.log(producto2);
console.log(producto3);
