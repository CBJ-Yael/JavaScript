// QUERY SELECTOR ALL
const cards = document.querySelectorAll('.card'); // nodeList con los elementos
console.log(cards);

// ID
const formularios = document.querySelectorAll('#formulario'); // tambien trae los dos IDs
console.log(formularios);

// NO EXISTE
const noExiste = document.querySelectorAll('.noExiste'); // nodeList vacio
console.log(noExiste);